# decorators

Учебный репозиторий с примерами использования декораторов в Python. 
В проекте реализованы примеры перехвата стандартного вывода (`sys.stdout`) для добавления временных меток и перенаправления вывода в файлы.

## 📋 Особенности

Проект демонстрирует решение следующих задач:
1. **Перехват `print()`**: Как изменить поведение функции `print` без изменения её вызова.
2. **Декораторы**:
   - `@timed_output` — добавляет временную метку `[YYYY-MM-DD HH:MM:SS]` к каждому выводу в консоль.
   - `@write_to_file` — перенаправляет весь вывод функции в указанный файл.
